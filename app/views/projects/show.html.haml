= @project.title
= link_to 'edit', edit_project_path(@project)
= link_to 'delete', project_path(@project), :method => :delete

%br
%br

Tasks
= link_to 'Add task', new_project_task_path(@project)
- @project.tasks.each do |t|
  = link_to t.title, project_task_path(t)
